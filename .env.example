# OS Data Hub API key, restricted to this app's domain (osdatahub.os.uk > Project > API keys)
VITE_OS_API_KEY=

# Password required to enter the app (checked by netlify/edge-functions/gate.ts)
GATE_PASSWORD=

# Optional: separate secret for signing the auth cookie. Defaults to GATE_PASSWORD if unset.
GATE_SECRET=
